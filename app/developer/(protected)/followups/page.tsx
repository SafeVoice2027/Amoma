// See app/admin/(protected)/followups/page.tsx — shared followups list,
// re-exported here so a Developer viewer sees it at /developer/followups
// instead of /admin/followups.
export { default } from "@/app/admin/(protected)/followups/page";
