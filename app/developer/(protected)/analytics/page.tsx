// See app/admin/(protected)/analytics/page.tsx — no basePath-dependent
// links on this page, so a plain re-export is all that's needed here.
export { default } from "@/app/admin/(protected)/analytics/page";
