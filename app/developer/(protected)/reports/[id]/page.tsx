// See app/admin/(protected)/reports/[id]/page.tsx — shared report detail,
// re-exported here so a Developer viewer sees it at /developer/reports/[id]
// instead of /admin/reports/[id].
export { default } from "@/app/admin/(protected)/reports/[id]/page";
