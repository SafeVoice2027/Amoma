// See app/admin/(protected)/reports/page.tsx — shared report list,
// re-exported here so a Developer viewer sees it at /developer/reports
// instead of /admin/reports.
export { default } from "@/app/admin/(protected)/reports/page";
