// See app/admin/(protected)/page.tsx — shared Case overview, re-exported
// here so a Developer viewer sees it at /developer instead of /admin.
export { default } from "@/app/admin/(protected)/page";
